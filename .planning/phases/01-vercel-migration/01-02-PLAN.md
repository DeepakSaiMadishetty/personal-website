---
phase: 01-vercel-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: false
requirements:
  - SITE-01
  - SITE-02
  - SITE-03

must_haves:
  truths:
    - "Visiting the Vercel production URL loads the site with no white screen and no 404s on CSS/JS"
    - "Navigating directly to /about and /projects loads the correct pages (not a Vercel 404)"
    - "GitHub Actions ran the Vercel deploy job successfully (green check)"
    - "GitHub Pages is disabled and no longer receives new deployments"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Proof Vercel project was linked (local only, gitignored)"
      contains: "projectId"
  key_links:
    - from: "development branch push"
      to: "Vercel production deployment"
      via: "GitHub Actions deploy.yml → vercel --prod"
      pattern: "vercel --prod"
---

<objective>
Push the code changes to `development`, verify the Vercel deployment succeeds, and confirm the live site works correctly — correct asset paths, working React Router routes, and CI pipeline green.

Purpose: Validate all three Phase 1 requirements are satisfied in production before closing the phase.
Output: Working live site at a *.vercel.app production URL.
</objective>

<execution_context>
@/Users/deepak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-vercel-migration/01-CONTEXT.md
@.planning/phases/01-vercel-migration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push to development</name>
  <files>.github/workflows/deploy.yml, vite.config.js, vercel.json</files>
  <action>
    Commit all three changed/created files and push to the development branch:

    ```bash
    git add vite.config.js vercel.json .github/workflows/deploy.yml
    git commit -m "feat(01-vercel-migration): migrate deployment from GitHub Pages to Vercel"
    git push origin development
    ```

    Do NOT use git add -A or git add . — only stage these three specific files.
    Do NOT include .vercel/ directory if present — it must remain gitignored.

    After pushing, note the GitHub Actions run URL from the push output or visit:
    https://github.com/[your-username]/personal-website/actions
  </action>
  <verify>
    <automated>cd /Users/deepak/projects/personal-website && git log --oneline -1 && git status</automated>
  </verify>
  <done>
    - Commit exists with all three files
    - git status shows clean working tree
    - Push succeeded (no rejected refs)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live Vercel deployment</name>
  <action>
    Observe the GitHub Actions run and verify the live Vercel deployment using the steps below.
  </action>
  <verify>
    <automated>MISSING — requires live deployment: curl -I https://[your-project].vercel.app returns HTTP 200</automated>
  </verify>
  <done>GitHub Actions "Deploy to Vercel" workflow ran green. Live *.vercel.app URL returns site with correct assets. All React Router routes load when navigated directly. GitHub Pages is disabled.</done>
  <what-built>
    Pushed three files to development: updated vite.config.js (base='/'), new vercel.json (SPA rewrite), replaced deploy.yml (Vercel CLI). GitHub Actions should have triggered a Vercel production deployment.
  </what-built>
  <how-to-verify>
    1. CHECK GitHub Actions:
       Visit https://github.com/[your-username]/personal-website/actions
       Confirm the "Deploy to Vercel" workflow ran (not "Deploy to GitHub Pages").
       The job should show a green checkmark. If it is still running, wait for it.

    2. GET the Vercel URL:
       Visit https://vercel.com/dashboard
       Find the personal-website project. The production URL is shown as *.vercel.app.
       (Or check the GitHub Actions log — the vercel --prod command prints the URL on success.)

    3. VERIFY homepage loads:
       Visit the *.vercel.app URL in a browser.
       Expected: Site loads with correct content. No white screen. No browser console 404s for CSS/JS files.
       Check browser DevTools Network tab — all /assets/*.js and /assets/*.css should return 200 (not 404).

    4. VERIFY React Router routes work:
       Paste these URLs directly in the browser address bar (do NOT use in-app navigation):
         https://[your-project].vercel.app/about
         https://[your-project].vercel.app/projects
       Expected: Each route loads the correct page (not a Vercel "404: NOT_FOUND" page).
       If you get a Vercel 404, vercel.json was not deployed correctly — report this.

    5. VERIFY asset paths are root-relative:
       View page source (Cmd+U / Ctrl+U) on the homepage.
       Confirm script/link tags reference /assets/... (NOT /personal-website/assets/...).

    6. DISABLE GitHub Pages (prevents stale old URL from confusing things):
       Visit https://github.com/[your-username]/personal-website/settings/pages
       Set Source to "None" and save.
       The old deepak.github.io/personal-website/ URL will eventually stop working.
  </how-to-verify>
  <resume-signal>Type "verified" if all checks pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when all of the following are true:
- GitHub Actions "Deploy to Vercel" workflow ran green on the development push
- Live *.vercel.app URL returns HTTP 200 with the site fully loaded
- Navigating directly to /about and /projects loads the page (not a 404)
- Asset URLs in page source use /assets/... not /personal-website/assets/...
- GitHub Pages is disabled in repository settings
</verification>

<success_criteria>
SITE-01: Site deployed on Vercel — live *.vercel.app URL returns the portfolio site
SITE-02: Vite base path is / — no /personal-website/ prefix in any built assets
SITE-03: GitHub Actions workflow deploys to Vercel on push to development
</success_criteria>

<output>
After completion, create `.planning/phases/01-vercel-migration/01-02-SUMMARY.md`
</output>
